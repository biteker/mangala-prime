version: '3.8'

services:
  mangala-app:
    image: ghcr.io/biteker/mangala-prime:latest
    container_name: mangala-backend
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - web-network

networks:
  web-network:
    external: true
